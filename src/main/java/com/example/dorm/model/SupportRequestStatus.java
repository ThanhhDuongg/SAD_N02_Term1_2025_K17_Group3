package com.example.dorm.model;

/**
 * Enumerates the lifecycle states of a support request submitted by a student.
 */
public enum SupportRequestStatus {
    PENDING,
    IN_PROGRESS,
    RESOLVED,
    REJECTED
}
